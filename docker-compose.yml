version: "3.8"

services:
  bot:
    image: botkit-support:1.0.0
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:8084:8084"
    read_only: true
    user: 1001:1001
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
